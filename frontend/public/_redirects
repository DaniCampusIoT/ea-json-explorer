# Railway sirve el frontend con su propio dominio.
# El frontend llama al backend mediante VITE_API_URL en tiempo de build.
# Este archivo es solo fallback SPA para Cloudflare Pages si se usa.
/*  /index.html  200
